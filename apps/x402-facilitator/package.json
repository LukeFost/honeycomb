{
  "name": "honeycomb-x402-facilitator",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Self-hosted x402 facilitator: verifies + settles EIP-3009 USDC payments on Ethereum mainnet (eip155:1) where the relayer EOA pays gas. No hosted facilitator supports ETH L1, so the Summon-a-TEE web route points at this.",
  "scripts": {
    "dev": "bun run server.ts",
    "supported": "bun run probe.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@x402/core": "^2.15.0",
    "@x402/evm": "^2.15.0",
    "viem": "^2.21.0"
  },
  "devDependencies": {
    "@types/bun": "^1.1.0",
    "typescript": "5.9.3"
  }
}
